<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (c) Frog Development 2015.
  -->

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx.xsd
	    http://www.springframework.org/schema/context
	    http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enable annotation scanning. -->
    <context:annotation-config/>

    <context:component-scan base-package="fr.frogdevelopment.assoplus"/>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource"/>
    </bean>

    <!-- DataSource -->
    <bean id="dataSource" class="fr.frogdevelopment.assoplus.datasource.RoutingDataSource">
        <property name="targetDataSources">
            <map key-type="fr.frogdevelopment.assoplus.datasource.RoutingDataSource$DataSource">
                <entry key="SQLITE" value-ref="sqliteDataSource"/>
                <entry key="MYSQL" value-ref="mysqlDataSource"/>
            </map>
        </property>
        <property name="defaultTargetDataSource" ref="sqliteDataSource"/>
    </bean>

    <bean id="sqliteDataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="org.sqlite.JDBC"/>
        <property name="url" value="jdbc:sqlite:assoplus.db"/>
        <property name="validationQuery" value="SELECT 1"/>
    </bean>

    <bean id="mysqlDataSource" class="com.mysql.jdbc.jdbc2.optional.MysqlDataSource">
        <property name="serverName" value="127.0.0.1"/>
        <property name="databaseName" value="asso_plus"/>
        <property name="user" value="root"/>
        <property name="password" value="root"/>
    </bean>

</beans>